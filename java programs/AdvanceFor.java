package NewPackage;

public class AdvanceFor {
public static void main(String[] args) {
	//for each loop && enhaced for loop is used hytrate ture element of array some collections it is also known as for-each loop.
	//for(datatype item:array)
	int a[]= {1,2,3,4};
	int sum=0;
	for(int number:a) {
		sum+=number;
		
	}
	System.out.println(sum);
}
}
